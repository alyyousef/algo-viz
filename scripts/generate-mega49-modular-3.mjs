import fs from 'fs/promises'
import path from 'path'

const TICK3 = '\`\`\`'
const TICK1 = '\`'

const contentMap = {
  'src/features/kb/routes/KB/25. Machine Learning/25.2 Algorithms/Gaussian mixture models/index.mdx': `---
title: Gaussian Mixture Models (GMM)
description: A probabilistic unsupervised clustering algorithm that mathematically models clusters as a mixture of multiple Gaussian distributions.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="Gaussian Mixture Models (GMM)">

K-Means clustering is mathematically rigid. It draws a strict circular boundary and assigns a data point to exactly one cluster (Hard Clustering). 

**Gaussian Mixture Models (GMM)** use probability calculus to perform **Soft Clustering**. It mathematically assumes that the dataset is generated by a mixture of multiple Gaussian (Normal) distributions, and calculates the exact *probability* that a data point belongs to a specific cluster.

## 1. The Mathematical Assumption
If you have a dataset of human heights, you might mathematically assume it is generated by two overlapping Gaussian distributions: one for Men, one for Women.
A GMM mathematically defines each cluster using three parameters:
- **Mean ($\mu$)**: The center of the bell curve.
- **Covariance ($\Sigma$)**: The mathematical width and shape of the bell curve (allowing the cluster to be an elongated oval, not just a perfect sphere).
- **Mixing Probability ($\pi$)**: The size/weight of the cluster (e.g., 60% of the dataset belongs to Cluster 1).

## 2. Expectation-Maximization (EM) Algorithm
Because the algorithm does not know the Mean, Covariance, or the Labels, it is caught in a mathematical paradox. It solves this using the **Expectation-Maximization (EM)** algorithm.

1. **Initialization**: Randomly guess the Means and Covariances of $K$ Gaussians.
2. **Expectation Step (E-Step)**: Use Bayes' Theorem to mathematically calculate the probability that every single data point belongs to each Gaussian. (e.g., Point A has a 70% chance of being in Cluster 1, and 30% for Cluster 2).
3. **Maximization Step (M-Step)**: Mathematically update the Mean and Covariance of the Gaussians to perfectly fit the probabilistic assignments calculated in the E-Step.
4. **Repeat**: Loop E and M until the Gaussians mathematically stop moving (Convergence).

## 3. Advantages over K-Means
- **Covariance Flexibility**: K-Means mathematically forces clusters to be spherical. GMMs mathematically allow clusters to be long, stretched ellipses, perfectly wrapping around elliptical data.
- **Probabilistic Output**: If a data point is on the border of two clusters, K-Means violently forces it into one. GMM outputs the mathematical nuance: "There is a 51% chance it is Cluster A, and 49% chance it is Cluster B."

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/25. Machine Learning/25.2 Algorithms/Gradient boosting/index.mdx': `---
title: Gradient Boosting
description: The foundational mathematical framework for sequentially chaining weak algorithms, where each new model is explicitly trained to minimize the residual errors of the previous ensemble.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="Gradient Boosting">

While Random Forests build 1,000 deep trees independently in parallel, **Gradient Boosting** builds 1,000 shallow trees sequentially. It is a mathematical optimization algorithm that treats Machine Learning as a game of golf: the first tree takes a massive swing to get close to the hole, and every subsequent tree takes a tiny putt to mathematically close the remaining distance (the error).

## 1. The Mathematical Engine (Residuals)
1. **Tree 1 (The Initial Guess)**: The algorithm mathematically predicts the average of the target variable for every single row. The error is massive.
2. **Calculating Residuals**: The algorithm calculates the exact mathematical difference between the true value and Tree 1's prediction. (True = 100, Predicted = 60, Residual = 40).
3. **Tree 2**: Here is the brilliant mathematical leap. Tree 2 is *not* trained to predict the target (100). Tree 2 is mathematically trained *only to predict the Residual (40)*. 
4. **The Ensemble**: To get the final prediction, you mathematically sum the output of the trees: $Prediction = Tree_1 + (Learning\_Rate \times Tree_2)$. 

## 2. Gradient Descent in Function Space
Why is it called *Gradient* Boosting? 
Because predicting the Residuals is mathematically identical to calculating the negative gradient of the Mean Squared Error (MSE) Loss Function. 

The algorithm is mathematically executing **Gradient Descent**. But instead of updating numerical weights (like a Neural Network), it is updating the prediction by mathematically adding a new Tree (a function) that points in the exact direction of the steepest descent toward zero error.

## 3. The Need for Shrinkage (Learning Rate)
If Tree 2 perfectly predicts the Residual of 40, the error immediately drops to 0, and the model mathematically overfits instantly. 
To prevent this, Gradient Boosting uses a mathematical hyperparameter called the **Learning Rate (Shrinkage)**. 
Instead of adding the full value of Tree 2, it multiplies it by a tiny number (e.g., 0.1). $60 + (40 \times 0.1) = 64$. 
The algorithm is mathematically forced to take 1,000 tiny, highly constrained steps to reach the target, resulting in unparalleled accuracy and robustness on tabular data.

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/25. Machine Learning/25.2 Algorithms/HDBSCAN/index.mdx': `---
title: HDBSCAN
description: An advanced hierarchical density-based clustering algorithm that mathematically eliminates the fatal flaw of DBSCAN by dynamically adapting to clusters of varying densities.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="HDBSCAN">

DBSCAN is a brilliant clustering algorithm, but it has one fatal mathematical flaw: the $\epsilon$ (Epsilon) radius is globally fixed. If a dataset contains a highly dense cluster of points, and a second, very loose, "fluffy" cluster of points, DBSCAN mathematically fails. If you set $\epsilon$ small, the fluffy cluster shatters into noise. If you set $\epsilon$ large, the dense clusters merge into a single blob.

**HDBSCAN (Hierarchical DBSCAN)** solves this mathematically by sweeping through all possible values of $\epsilon$ simultaneously.

## 1. Core Distance and Mutual Reachability
Instead of using raw Euclidean distance, HDBSCAN mathematically transforms the space using **Mutual Reachability Distance**. 
It calculates the "Core Distance" (how far you have to travel to find $K$ neighbors). The Mutual Reachability Distance between Point A and Point B is mathematically defined as the absolute maximum of:
1. Core Distance of A
2. Core Distance of B
3. Raw distance between A and B

This mathematical transformation forcefully pushes outliers far away from the dense regions, making the true clusters much more mathematically pronounced.

## 2. Building the Minimum Spanning Tree
HDBSCAN mathematically links all points together using a Minimum Spanning Tree (MST), where the edges are weighted by the Mutual Reachability Distance. 
It then systematically sorts the edges by weight and starts mathematically "cutting" the heaviest edges first. This organically builds a massive **Cluster Hierarchy (Dendrogram)**, simulating the effect of running DBSCAN with an $\epsilon$ that shrinks continuously from infinity down to zero.

## 3. Cluster Extraction (The Condensation)
The final brilliant mathematical step is extracting the clusters from the massive tree. 
Instead of a human cutting the tree horizontally, HDBSCAN mathematically defines cluster "Stability" over volume. It asks: *"Does this cluster persist for a long time as $\epsilon$ shrinks, or does it immediately shatter into noise?"* 
It mathematically selects the most persistent, stable clusters, perfectly isolating clusters of vastly varying densities without requiring the human to guess the $\epsilon$ parameter.

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/25. Machine Learning/25.2 Algorithms/Hierarchical clustering/index.mdx': `---
title: Hierarchical Clustering
description: An unsupervised learning algorithm that mathematically builds a nested tree of clusters (a dendrogram), allowing humans to choose the optimal number of clusters post-training.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="Hierarchical Clustering">

K-Means requires you to mathematically guess the number of clusters ($K$) before you run the algorithm. **Hierarchical Clustering** does the opposite. It mathematically builds a massive tree containing *every possible number of clusters* from 1 to $N$, allowing you to inspect the tree and choose the perfect $K$ after the math is done.

## 1. Agglomerative (Bottom-Up)
The most common mathematical approach.
1. Start with 1,000 data points. Assume there are 1,000 clusters (every point is its own cluster).
2. Mathematically calculate the distance matrix between all 1,000 clusters.
3. Find the two clusters that are mathematically closest together, and merge them into a single cluster. (You now have 999 clusters).
4. Repeat this mathematical loop 999 times until all data points are merged into a single massive super-cluster.

## 2. Linkage Criteria (The Math of Merging)
How do you mathematically calculate the distance between two clusters if each cluster contains 50 points?
- **Single Linkage**: The distance is the mathematical minimum distance between any point in Cluster A and any point in Cluster B. (Tends to create long, stringy clusters).
- **Complete Linkage**: The distance is the maximum distance between the points. (Tends to create tight, compact spheres).
- **Ward's Method**: The industry standard. It mathematically calculates the variance inside the clusters. It merges the two clusters that result in the absolute minimum increase in total internal variance.

## 3. The Dendrogram
The algorithm outputs a **Dendrogram**, a massive mathematical tree diagram. 
The y-axis represents the mathematical distance at which the clusters were merged. 
A Data Scientist inspects the dendrogram, looks for a massive vertical gap (a huge jump in mathematical distance), and simply draws a horizontal line through the tree to mathematically extract the optimal number of clusters.

*Downside*: It requires building an $N \times N$ distance matrix, giving it an $O(N^3)$ time complexity, making it computationally impossible to run on datasets larger than a few tens of thousands of rows.

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/25. Machine Learning/25.2 Algorithms/Isolation Forest/index.mdx': `---
title: Isolation Forest
description: An ingenious, tree-based unsupervised algorithm mathematically optimized specifically to isolate and detect anomalies rather than profile normal data.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="Isolation Forest">

Most anomaly detection algorithms (like Autoencoders or One-Class SVMs) attempt to mathematically profile "normal" data, and flag anything that doesn't fit. 

**Isolation Forests** completely invert the mathematical paradigm. They do not care about normal data. They are explicitly mathematically designed to actively target and isolate the anomalies directly.

## 1. The Mathematical Premise (Anomalies are Easy to Isolate)
Anomalies, by mathematical definition, are "few and different". Because they are far away from the dense clusters of normal data, they are incredibly easy to isolate.

If you have a dense cluster of 1,000 normal transactions, and 1 massive fraudulent transaction far away on the graph:
- If you draw random lines (splits) through the data, the fraudulent transaction will be isolated into its own mathematical bucket after only 2 or 3 random splits.
- The normal transactions will require 20 or 30 random splits before they are finally isolated from each other because they are so densely packed.

## 2. The Algorithmic Flow
1. The algorithm builds 100 **Isolation Trees**.
2. Unlike a Decision Tree which calculates Information Gain, an Isolation Tree is mathematically random. It picks a random feature, picks a random threshold, and splits the data. It repeats this recursively until every point is isolated.
3. For every single data point, the algorithm mathematically counts the **Path Length** (how many splits it took to isolate that specific point).
4. It averages the Path Length across all 100 trees.

## 3. The Anomaly Score
The anomaly score is mathematically derived directly from the Path Length.
- If the average Path Length is incredibly short (e.g., 2.5), the point is mathematically an **Anomaly**. It was insanely easy to isolate.
- If the average Path Length is long (e.g., 25.0), the point is buried deep inside a dense cluster. It is mathematically **Normal**.

Because it relies purely on random splits and does not calculate distance matrices, Isolation Forest has an incredibly fast $O(N)$ time complexity, making it the absolute industry standard for anomaly detection on massive real-time streams (like credit card fraud).

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/25. Machine Learning/25.2 Algorithms/K-Means/index.mdx': `---
title: K-Means Clustering
description: The foundational unsupervised clustering algorithm that mathematically partitions data into K distinct spherical clusters based on geometric centroids.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="K-Means Clustering">

**K-Means** is the most famous, heavily utilized, and mathematically simple unsupervised clustering algorithm in existence. It is designed to partition an unlabeled dataset into $K$ distinct, non-overlapping clusters.

## 1. The Mathematical Objective
The mathematical goal of K-Means is to minimize the **Within-Cluster Sum of Squares (WCSS)**. 
It attempts to mathematically ensure that the distance from every data point to its assigned cluster center (Centroid) is as small as possible, creating incredibly tight, dense clusters.

## 2. Lloyd's Algorithm (The Loop)
Because finding the absolute perfect mathematical clusters is NP-Hard (computationally impossible for large data), K-Means uses an iterative heuristic called Lloyd's Algorithm:
1. **Initialization**: The user explicitly defines $K$. The algorithm places $K$ random Centroids in the high-dimensional space.
2. **Assignment Step**: Using Euclidean Distance, the algorithm mathematically calculates the distance from every single data point to all $K$ centroids. It assigns the point to the closest centroid.
3. **Update Step**: The algorithm mathematically calculates the exact mean (average) coordinates of all the points assigned to Cluster 1. It physically moves the Centroid for Cluster 1 to that exact coordinate.
4. **Convergence**: It loops the Assignment and Update steps. The centroids mathematically drift toward the dense areas of the data. When the centroids stop moving, the algorithm terminates.

## 3. The Elbow Method (Finding K)
The fatal flaw of K-Means is that it mathematically forces you to guess $K$. If there are 3 real clusters, but you set $K=10$, it will violently chop the data into 10 meaningless pieces.

To find the mathematically optimal $K$, Data Scientists use the **Elbow Method**. They run K-Means for $K=1$, $K=2$, $K=3...$, calculating the WCSS Loss for each. They plot the Loss on a graph. The curve rapidly plunges, then abruptly flattens out, forming an "Elbow". The exact mathematical point of the Elbow represents the optimal number of clusters before the algorithm starts overfitting the noise.

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/25. Machine Learning/25.2 Algorithms/KNN/index.mdx': `---
title: K-Nearest Neighbors (KNN)
description: A mathematically pure, non-parametric algorithm that performs classification and regression by executing distance calculations directly against the raw training data.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="K-Nearest Neighbors (KNN)">

**K-Nearest Neighbors (KNN)** is known as a "Lazy Learning" algorithm. Unlike a Neural Network, KNN has mathematically zero training phase. It does not calculate weights or gradients. It simply memorizes the entire dataset, and executes all of its mathematics exclusively at the exact moment of prediction (inference).

## 1. The Core Mathematics (Distance)
If you want to classify a new, unknown data point, KNN relies purely on mathematical geometry.
1. It calculates the strict mathematical distance (usually **Euclidean Distance**) from the new point to *every single row* in the historical dataset.
2. It sorts the distances, and retrieves the top $K$ mathematically closest historical points (the "Neighbors").
3. **For Classification**: The $K$ neighbors vote. If $K=5$, and 4 neighbors are "Dog" and 1 is "Cat", the algorithm mathematically outputs "Dog".
4. **For Regression**: It takes the mathematical average of the target variables of the $K$ neighbors.

## 2. The Curse of Dimensionality
KNN is highly susceptible to the **Curse of Dimensionality**. 
As the number of features (columns) increases past 15 or 20, the mathematical volume of the space expands exponentially. In high-dimensional space, the mathematical distance between *any* two points approaches equality. The concept of a "Nearest Neighbor" mathematically collapses, and the algorithm becomes equivalent to random guessing. PCA (Dimensionality Reduction) must be applied before using KNN on wide data.

## 3. Time Complexity and KD-Trees
Because it has zero training time, KNN's prediction time is catastrophic. To predict one point, it must mathematically calculate the distance to all 10 million historical points, making it $O(N)$.

To solve this in production, engineers use specialized spatial data structures like **KD-Trees** or **Ball Trees**. These structures mathematically partition the dataset during "training", allowing the prediction algorithm to ignore 99% of the historical points, reducing the mathematical search time from $O(N)$ to $O(\log N)$.

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/25. Machine Learning/25.2 Algorithms/Lasso/index.mdx': `---
title: Lasso Regression (L1)
description: A variant of Linear Regression that mathematically utilizes an absolute value penalty to automatically perform feature selection by shrinking coefficients to zero.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="Lasso Regression (L1)">

**Lasso (Least Absolute Shrinkage and Selection Operator)** is a powerful Linear Regression algorithm designed to solve datasets that have massive amounts of irrelevant features, by applying L1 Regularisation.

## 1. The Mathematical Objective
Standard Linear Regression minimizes the Mean Squared Error (MSE). Lasso modifies this objective function by adding a mathematical penalty:
$Loss = MSE + \lambda \sum |w_i|$

- **$\lambda$ (Alpha)**: The hyperparameter controlling the strength of the penalty.
- **$|w_i|$**: The absolute value of the mathematical weights (slopes) of the features.

## 2. The Geometry of Feature Selection (Why weights go to zero)
L2 Regularisation (Ridge) shrinks weights, but rarely to zero. L1 (Lasso) shrinks weights to *exactly* zero. Why? It is pure geometry.

Imagine a 2D graph of the Loss Function. 
- The L2 penalty is mathematically a smooth **Circle**. When the gradient descent optimizer expands to touch the circle, it usually hits the edge (meaning both $w_1$ and $w_2$ have non-zero values).
- The L1 penalty is mathematically a sharp **Diamond** (because of the absolute value function). The diamond has sharp, pointed corners exactly on the axes. When the optimizer expands to touch the diamond, it almost mathematically always hits a sharp corner. 
If it hits the corner on the $y$-axis, the $x$-coordinate is strictly $0.0$. The algorithm has mathematically deleted that feature.

## 3. Production Use Cases
Lasso is absolutely vital in fields like Bioinformatics (Genomics). 
If you are predicting a disease, you might have 50,000 columns (representing 50,000 genes), but only 100 patients. Standard regression mathematically crashes (infinite solutions).
Lasso flawlessly handles this. The mathematical diamond penalty aggressively forces 49,990 genes to exactly 0.0, outputting a highly interpretable model that explicitly tells the doctors which 10 genes actually cause the disease.

</ConceptTemplate>
`,
}

async function main() {
  for (const [filePath, content] of Object.entries(contentMap)) {
    const fullPath = path.resolve(filePath)
    await fs.mkdir(path.dirname(fullPath), { recursive: true })

    // Safely replace TICK1 and TICK3 placeholders with actual backticks
    let finalContent = content.replace(/TICK3/g, TICK3).replace(/TICK1/g, TICK1)

    // Append a safe newline
    await fs.writeFile(fullPath, finalContent.trim() + '\n', 'utf-8')
    console.log('Wrote ' + filePath)
  }
}

main().catch(console.error)
