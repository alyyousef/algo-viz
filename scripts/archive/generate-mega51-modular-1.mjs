import fs from 'fs/promises'
import path from 'path'

const TICK3 = '```'
const TICK1 = '`'

const contentMap = {
  'src/features/kb/routes/KB/26. Deep Learning/Learning rate schedules/index.mdx': `---
title: Learning Rate Schedules
description: Advanced techniques for dynamically adjusting the learning rate during training, allowing models to converge faster and escape local minima.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="Learning Rate Schedules">

If you use a **Learning Rate** that is too high, the model will mathematically bounce over the valley and never converge. If it is too low, training will take thousands of years. 
**Learning Rate Schedules** solve this by mathematically shifting the learning rate *during* the training process.

## 1. Step Decay
The simplest mathematical schedule. You start with a high learning rate (e.g., $0.1$) to blast through the early stages of training. Every 10 epochs, you multiply the learning rate by a fraction (e.g., $0.5$). 
This causes the algorithm to take massive leaps early on, but take mathematically tiny, precise steps as it approaches the final global minimum.

## 2. Cosine Annealing
Instead of harsh, sudden drops, **Cosine Annealing** mathematically lowers the learning rate following the smooth curve of a cosine function. 
It starts high, gently curves downward, and eventually approaches zero. This is the absolute standard schedule for training modern Large Language Models and Vision Transformers, as the smooth mathematical decay perfectly stabilizes complex loss landscapes.

## 3. Warmups
When using massive batch sizes or complex optimizers like AdamW, the first few training steps are mathematically chaotic because the momentum trackers haven't collected any data yet. 
If the learning rate is high, the model might permanently destroy itself in the first 10 steps. **Warmups** mathematically force the learning rate to start at exactly $0$, and linearly increase to the maximum rate over the first few epochs, allowing the momentum to safely stabilize before real training begins.

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/26. Deep Learning/Loss functions/index.mdx': `---
title: Loss Functions
description: The mathematical formulas that calculate exactly how wrong a model's predictions are, generating the error signal required for Gradient Descent.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="Loss Functions">

A Neural Network is essentially a massive mathematical function. But to train it, you must be able to mathematically quantify exactly how "bad" its current predictions are. This single number is generated by the **Loss Function** (or Objective Function).

## 1. Regression: Mean Squared Error (MSE)
If the model is predicting a continuous number (like House Prices), the standard loss function is **MSE**.
$L = \frac{1}{n} \sum (\hat{y} - y)^2$
By squaring the error, MSE mathematically creates a smooth U-shaped curve, ensuring that there is a perfect, calculable derivative pointing exactly to the lowest error. It also heavily punishes massive outliers.

## 2. Binary Classification: Binary Cross-Entropy (BCE)
If the model is predicting a 1 or 0 (like Spam or Not Spam), it outputs a probability (e.g., $0.9$). You cannot use MSE for probabilities because the resulting loss landscape becomes mathematically bumpy (non-convex). 
**BCE** uses logarithms to calculate the error:
$L = - [y \log(\hat{y}) + (1 - y) \log(1 - \hat{y})]$
If the true answer is $1$, and the model predicts $0.001$, the logarithm mathematically explodes toward infinity, instantly generating a massive error signal that forces the model to correct itself.

## 3. Multi-Class: Categorical Cross-Entropy
When predicting between 10 different categories (like identifying digits 0-9), the model outputs 10 probabilities that must sum to 1.0 (via Softmax). **Categorical Cross-Entropy** mathematically compares this predicted probability distribution against the true distribution (a one-hot encoded vector), heavily punishing the network if it assigned a low probability to the correct class.

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/26. Deep Learning/Mixed-precision training/index.mdx': `---
title: Mixed-Precision Training
description: A hardware-level technique that dramatically speeds up training by dynamically swapping between 16-bit and 32-bit floats.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="Mixed-Precision Training">

Historically, Deep Learning mathematically processed all numbers as **FP32 (32-bit floats)**. This offered massive mathematical precision, but consumed enormous amounts of VRAM and bottlenecked GPU calculation speeds. 
**Mixed-Precision Training** is a revolutionary hardware technique that dynamically mixes FP16 and FP32, doubling training speed and halving VRAM usage with zero loss in final accuracy.

## 1. The Mathematical Math (FP16)
The forward pass and backward pass (calculating the massive matrix multiplications) do not require microscopic mathematical precision. 
In Mixed-Precision, the GPU mathematically downcasts the weights and activations into **FP16 (16-bit floats)**. 
Because FP16 matrices are exactly half the size, the GPU's highly specialized Tensor Cores can multiply them at blazing speeds, slashing training time by up to 50%.

## 2. The Master Weights (FP32)
If FP16 is so fast, why not use it for everything?
Because during the final step of Gradient Descent, the optimizer subtracts a microscopic learning rate from the weight. FP16 simply does not have the decimal precision to mathematically represent numbers that small. The weight update would become zero, and the model would stop learning.
Mixed-Precision solves this by keeping a master copy of the weights in **FP32**. The microscopic updates are applied mathematically to the FP32 master weights, which are then downcasted back to FP16 for the next blazing-fast forward pass.

## 3. Gradient Scaling
Because FP16 cannot mathematically represent extremely small numbers, tiny gradients can "underflow" and become absolute zero. To fix this, the framework mathematically multiplies all gradients by a massive number (e.g., $1000$) before converting to FP16, calculates the math, and then mathematically divides by $1000$ before updating the FP32 weights.

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/26. Deep Learning/Model pruning/index.mdx': `---
title: Model Pruning
description: A model compression technique that mathematically deletes unimportant neurons or weights from a trained network, drastically reducing inference cost.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="Model Pruning">

A fully trained 70-Billion parameter Neural Network contains massive amounts of "dead weight." Many specific weights are mathematically so close to zero that they have no actual impact on the final output. 
**Model Pruning** mathematically identifies these useless parameters and permanently deletes them, compressing the model for deployment on edge devices like smartphones.

## 1. Unstructured Pruning (Magnitude)
The most basic mathematical approach. The algorithm looks at every single individual weight in the massive matrices. If a weight has an absolute value very close to zero (e.g., $0.00001$), it mathematically forces it to absolute $0$. 
This creates highly sparse matrices. While this compresses the file size (using sparse storage formats), standard GPU hardware struggles to execute unstructured sparse math quickly, meaning it doesn't always speed up inference.

## 2. Structured Pruning
Instead of deleting random individual weights, **Structured Pruning** mathematically deletes entire structures (like a whole convolutional filter, or an entire attention head). 
Because the remaining matrices are perfectly dense (just smaller), standard GPUs can process them vastly faster. This guarantees a massive speedup in inference, though it risks damaging the model's accuracy more than unstructured pruning.

## 3. The Lottery Ticket Hypothesis
Why train a massive 70B model just to prune it down to 10B? Why not just train a 10B model from scratch?
The **Lottery Ticket Hypothesis** mathematically proved that within the massive random initialization of a 70B model, there exists a tiny "winning ticket" sub-network that is perfectly initialized to learn the task. The massive model is mathematically required just to guarantee you buy enough tickets to win. Once the winning ticket is trained, you can prune the rest of the network away.

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/26. Deep Learning/Multi-layer perceptrons/index.mdx': `---
title: Multi-Layer Perceptrons (MLP)
description: The foundational deep neural network architecture, consisting of a fully connected input layer, hidden layers, and an output layer.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="Multi-Layer Perceptrons (MLP)">

The **Multi-Layer Perceptron (MLP)** is the classic, absolute foundation of Deep Learning. Also known as a "Dense Network" or "Fully Connected Network," it mathematically proves that Artificial Intelligence can approximate any continuous function in the universe.

## 1. The Architecture
An MLP consists of three distinct mathematical structures:
1. **Input Layer**: A vector representing the raw data (e.g., 784 pixels of an image).
2. **Hidden Layers**: One or more layers in the middle. Every single neuron in a hidden layer is mathematically connected to every single neuron in the previous layer.
3. **Output Layer**: A final vector outputting the prediction (e.g., 10 probabilities for digit classification).

## 2. The Universal Approximation Theorem
A single Perceptron can only draw a straight line (Linear). By adding just one single Hidden Layer and applying a Non-Linear Activation Function (like ReLU or Sigmoid), the MLP gains the mathematical ability to bend and warp its decision boundaries. 
The **Universal Approximation Theorem** mathematically proves that an MLP with just a single hidden layer (with enough neurons) can perfectly approximate *any continuous mathematical function*, no matter how complex.

## 3. The Flaw (Spatial Awareness)
While MLPs are mathematically universal, they are terribly inefficient for unstructured data. 
If you feed a $1000 \times 1000$ image into an MLP, the first hidden layer requires a matrix of $1,000,000$ inputs. The number of weights mathematically explodes into the billions instantly. Furthermore, because it connects everything to everything, an MLP has zero mathematical concept of "spatial geometry" (it doesn't realize that two pixels next to each other form an edge). This flaw led to the invention of Convolutional Neural Networks (CNNs).

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/26. Deep Learning/Neural architecture search/index.mdx': `---
title: Neural Architecture Search (NAS)
description: An automated meta-learning process where an AI algorithm mathematically searches for and discovers the optimal neural network architecture for a specific dataset.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="Neural Architecture Search (NAS)">

Designing a neural network requires massive human intuition. How many layers? Should we use a $3\times 3$ or $5\times 5$ convolution? Should there be skip connections? 
**Neural Architecture Search (NAS)** mathematically removes the human from the equation. It uses an AI to design the AI.

## 1. The Search Space
The algorithm is first given a massive mathematical **Search Space**. This defines the Lego blocks the AI is allowed to use (e.g., specific convolutions, pooling layers, activation functions, and how they can be wired together). The space is theoretically infinite, containing trillions of possible unique architectures.

## 2. The Controller (Reinforcement Learning)
In classic NAS (like Google's AutoML), a Recurrent Neural Network (the Controller) mathematically generates a string representing a specific architecture. 
That architecture is instantly built, trained on the dataset for a few epochs, and evaluated for accuracy. 
The resulting accuracy score is mathematically fed back to the Controller as a **Reward** via Reinforcement Learning. If the architecture scored high, the Controller mathematically updates its internal weights to generate more architectures that look similar to the winning design.

## 3. Differentiable Search (DARTS)
The RL approach required thousands of GPU hours to evaluate millions of designs. 
**Differentiable Architecture Search (DARTS)** revolutionized NAS. Instead of trying discrete architectures one by one, DARTS mathematically constructs one massive "Super-Network" that contains *every possible connection* simultaneously. It applies a mathematical weight (a probability) to every single connection. 
During training, Gradient Descent mathematically pushes the weights of bad connections to $0$, and good connections to $1$. Once training is complete, the network simply prunes the $0$ connections, instantly revealing the mathematically perfect architecture in a fraction of the time.

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/26. Deep Learning/Perceptrons/index.mdx': `---
title: Perceptrons
description: The earliest and simplest mathematical model of a biological neuron, capable of learning simple linear decision boundaries.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="Perceptrons">

Invented in 1957, the **Perceptron** is the absolute foundational atom of Artificial Intelligence. It was the first mathematical attempt to replicate how a biological neuron fires in the human brain, serving as the direct ancestor to all modern Deep Learning.

## 1. The Mathematical Mechanism
A Perceptron is incredibly mathematically simple. It takes multiple inputs ($X$), multiplies them by specific weights ($W$), and adds them all together. 
$Z = (x_1 \cdot w_1) + (x_2 \cdot w_2) + \text{Bias}$

It then passes that sum through a harsh **Step Function**:
- If $Z > 0$, the Perceptron outputs $1$ (It "fires").
- If $Z \le 0$, the Perceptron outputs $0$ (It stays silent).

## 2. The Perceptron Learning Rule
Before Gradient Descent was invented, the Perceptron learned via a brutal, iterative algebraic rule. 
If it predicted $1$, but the true answer was $0$, it would mathematically subtract the inputs from its weights to force the sum lower. Over thousands of iterations, the Perceptron would mathematically rotate its decision boundary until it perfectly separated the two classes.

## 3. The XOR Crisis (The AI Winter)
In 1969, researchers mathematically proved a fatal flaw: The Perceptron can only draw a perfectly straight line (Linear Separability). 
It could successfully learn the logic for AND or OR gates. But it was mathematically impossible for a single Perceptron to learn the **XOR** (Exclusive OR) gate, because XOR points cannot be separated by a single straight line. 
This simple mathematical proof caused the first "AI Winter," freezing funding for neural networks for a decade until the invention of the Multi-Layer Perceptron and Backpropagation solved the nonlinear problem.

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/26. Deep Learning/RMSProp/index.mdx': `---
title: RMSProp
description: A critical optimization algorithm designed to solve the rapidly diminishing learning rates of AdaGrad by using a moving average of squared gradients.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="RMSProp (Root Mean Square Propagation)">

In Deep Learning, some parameters (like common words) are updated constantly, while others (like rare words) are updated rarely. **AdaGrad** was an early optimizer that assigned a unique learning rate to every parameter, dividing the rate by the sum of all past squared gradients. 
But AdaGrad had a fatal mathematical flaw: because it kept summing *all* past gradients forever, the denominator constantly grew, mathematically crushing the learning rate down to absolute $0$, stopping training entirely. **RMSProp** was invented to fix this.

## 1. The Moving Average Fix
Instead of keeping a permanent sum of all past history, RMSProp mathematically applies an **Exponentially Decaying Moving Average** to the squared gradients.

$E[g^2]_t = 0.9 \cdot E[g^2]_{t-1} + 0.1 \cdot g_t^2$

This equation mathematically forces the algorithm to heavily prioritize recent gradients and slowly "forget" the distant past. 

## 2. The Mathematical Normalization
When updating the weights, RMSProp mathematically divides the learning rate by the square root of that moving average.
- If a parameter has a massive, highly volatile gradient recently, the moving average is huge. RMSProp mathematically divides the learning rate by a huge number, heavily throttling the parameter to prevent it from exploding.
- If a parameter has been quiet recently, the moving average is tiny. RMSProp mathematically divides the learning rate by a tiny number, drastically accelerating the parameter to force it to learn.

## 3. Legacy
RMSProp was so historically effective that it was famously never published in a formal peer-reviewed paper; it was simply taught by Geoffrey Hinton in a Coursera lecture. It mathematically forms one half of the modern **Adam** optimizer (Adam is literally just RMSProp + Momentum).

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/26. Deep Learning/SGD/index.mdx': `---
title: SGD (Stochastic Gradient Descent)
description: The core optimization algorithm that updates weights using a single data point or small mini-batch at a time, drastically reducing mathematical computation.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="SGD (Stochastic Gradient Descent)">

Standard Gradient Descent requires calculating the error across the *entire dataset* before taking a single mathematical step. If your dataset has 10 million images, calculating a single gradient update could take weeks. 
**Stochastic Gradient Descent (SGD)** mathematically bypasses this bottleneck by relying on statistical approximations.

## 1. The "Stochastic" (Random) Element
Instead of looking at the entire dataset, true SGD picks exactly **one random data point**, calculates the mathematical gradient for that single point, and immediately updates the weights. 
Because it updates instantly, it can take 10 million mathematical steps in the time it takes standard Gradient Descent to take exactly one. 

## 2. The Noisy Path
Because it only looks at one random image at a time, the mathematical gradient is highly inaccurate (Noisy). 
If a random image of a dog happens to be blurry, SGD might mathematically update the weights in completely the wrong direction. However, across millions of iterations, these random errors mathematically cancel out, and the algorithm violently zig-zags its way down to the global minimum.

## 3. Mini-Batch SGD (The Industry Standard)
True SGD (batch size of 1) is too erratic and cannot mathematically utilize the massive parallel processing power of modern GPUs. 
The absolute industry standard is **Mini-Batch SGD**. It picks a random chunk of data (e.g., 64 or 256 images), calculates the gradient of that specific batch, and updates the weights. This mathematically perfectly balances the blazing speed of stochastic updates with the statistical stability of batch processing.

</ConceptTemplate>
`,

  'src/features/kb/routes/KB/26. Deep Learning/Sigmoid/index.mdx': `---
title: Sigmoid Activation Function
description: A classic activation function that mathematically squashes any real number into a strict probability between 0 and 1, famous for causing the vanishing gradient problem.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

<ConceptTemplate title="Sigmoid Activation Function">

In the early days of Deep Learning, the **Sigmoid** function was the absolute standard activation function. Because it creates a beautiful, smooth "S-curve" that mathematically squashes any input to a value between $0$ and $1$, it was considered the perfect mathematical representation of a biological neuron firing (or not firing).

## 1. The Mathematical Equation
$\sigma(x) = \frac{1}{1 + e^{-x}}$

- If you input a massive positive number ($+100$), the math forces the output to mathematically lock at exactly $0.9999$.
- If you input a massive negative number ($-100$), the math forces the output to exactly $0.0001$.
- At exactly $0$, the output is exactly $0.5$.

This makes Sigmoid the absolute mathematical standard for the final layer of a **Binary Classification** model (e.g., outputting an 85% probability that an email is Spam).

## 2. The Fatal Flaw (Vanishing Gradients)
While perfect for the final output layer, Sigmoid is mathematically catastrophic when used in the hidden layers of a Deep Neural Network. 
Look at the edges of the S-curve. If the input is $+10$, the curve is perfectly flat. The mathematical derivative (the slope) of a flat line is exactly $0$. 
During Backpropagation, Gradient Descent relies on multiplying these derivatives together. If the derivative is $0$ (or near $0$), the mathematical signal is instantly crushed to absolute zero. The network completely stops learning. This is the **Vanishing Gradient Problem**, and it is the exact reason ReLU replaced Sigmoid in hidden layers.

</ConceptTemplate>
`,
}

async function main() {
  for (const [filePath, content] of Object.entries(contentMap)) {
    const fullPath = path.resolve(filePath)
    await fs.mkdir(path.dirname(fullPath), { recursive: true })

    // Safely replace TICK1 and TICK3 placeholders with actual backticks
    let finalContent = content.replace(/TICK3/g, TICK3).replace(/TICK1/g, TICK1)

    // Append a safe newline
    await fs.writeFile(fullPath, finalContent.trim() + '\n', 'utf-8')
    console.log('Wrote ' + filePath)
  }
}

main().catch(console.error)
