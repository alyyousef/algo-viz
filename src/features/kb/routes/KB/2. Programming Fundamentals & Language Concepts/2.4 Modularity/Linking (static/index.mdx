---
title: Linking (Static) (Redirect)
description: "Redirecting to Static vs Dynamic Linking."
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'
import { Callout } from '@/features/kb/components/mdx/Callout'

export default function Layout({ children }) {
  return (
    <ConceptTemplate title="Static Linking">
      {children}
    </ConceptTemplate>
  )
}

<Callout icon="info" title="Redirect">
The architectural process of **Static Linking** (where the Linker physically embeds `.a` or `.lib` binaries directly into the final executable) is contrasted against DLLs in the Deep Dive for **Static vs. Dynamic Linking**. 

Please navigate to the [Static vs. Dynamic Linking](/kb/2.-programming-fundamentals--language-concepts/2.4-modularity/static-vs-dynamic-linking) module to explore binary compilation architecture.
</Callout>
