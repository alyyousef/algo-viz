---
title: Dynamic Linking (Redirect)
description: "Redirecting to Static vs Dynamic Linking."
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'
import { Callout } from '@/features/kb/components/mdx/Callout'

export default function Layout({ children }) {
  return (
    <ConceptTemplate title="Dynamic Linking">
      {children}
    </ConceptTemplate>
  )
}

<Callout icon="info" title="Redirect">
The architectural mechanism of **Dynamic Linking** (where the Operating System loads `.dll` or `.so` files into RAM at runtime rather than embedding them in the binary) is contrasted against Static Linking in the Deep Dive for **Static vs. Dynamic Linking**. 

Please navigate to the [Static vs. Dynamic Linking](/kb/2.-programming-fundamentals--language-concepts/2.4-modularity/static-vs-dynamic-linking) module to explore runtime binary loading.
</Callout>
