---
title: Modules
description: The modern architectural paradigm for mathematically grouping related code, data, and interfaces into a single, cohesive, pre-compiled unit.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

export default function Layout({ children }) {
  return (
    <ConceptTemplate title="Modules">
      {children}
    
  </ConceptTemplate>
  )
}

A Module is a mathematical step up from a simple File. It is a cohesive unit of software that explicitly defines what dependencies it requires (Imports) and exactly what API it provides to the outside world (Exports).

## 1. C++20 Modules vs Legacy Headers
For 40 years, C++ suffered from slow compilation due to Header files. C++20 introduced true **Modules**.
When a C++20 Module is compiled, the compiler does not generate raw text. It generates a highly compacted, mathematically optimized Binary representation of the Abstract Syntax Tree (the `.pcm` file).
When another file `import`s that module, the compiler simply loads the binary AST directly into memory. It does not have to parse text, mathematically eliminating the exponential compile-time explosion caused by `#include`.

## 2. Namespace Encapsulation
Modules inherently provide mathematical isolation.
In legacy systems, if File A defines a global function `init()`, and File B defines a global function `init()`, linking them together causes a catastrophic mathematical collision (Multiple Definition Error).
In a Module system, the functions are mathematically bound to the Module's internal scope. They are invoked as `ModuleA.init()`, preventing global namespace pollution.

