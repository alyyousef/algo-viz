---
title: Parametric Polymorphism
description: A highly advanced mathematical concept in type theory (implemented as Generics in Java/C# and Templates in C++) allowing developers to write classes and methods that operate perfectly on types that are not specified until instantiation.
---
import { ConceptTemplate } from '@/features/kb/components/templates/ConceptTemplate'

export default function Layout({ children }) {
  return (
    <ConceptTemplate
  title="Parametric Polymorphism"
  subtitle="Generics and Templates"
  tags={['OOP', 'Polymorphism', 'Type Theory', 'Generics']}
>
      {children}
    
  </ConceptTemplate>
  )
}

If you want to build a reusable `List` class that holds data, writing a `StringList` and an `IntegerList` requires mathematically duplicating the exact same array-resizing logic twice. This is an architectural failure.

## 1. The Generic Parameter
Parametric Polymorphism (Generics) solves this by allowing the Class itself to accept a mathematical variable representing a Type, usually denoted as `<T>`.
```java
public class List<T> {
    private T[] elements;
    public void add(T item) { ... }
}
```
The class is mathematically ignorant of what `T` actually is. It simply promises the compiler that whatever `T` is, it will handle it consistently.

## 2. Compile-Time Type Safety
When a developer instantiates the class: `List<String> myList = new List<>();`, the compiler mathematically substitutes `String` into the `T` variable throughout the entire class.
If the developer attempts to execute `myList.add(5)`, the compiler throws a fatal mathematical error. The developer achieved total code reusability without sacrificing a single degree of strict, compile-time type safety.

